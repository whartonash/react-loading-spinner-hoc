import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'


// Render our App component into the root element in the DOM
ReactDOM.render(
  <App />, 
  document.getElementById('root')
)
